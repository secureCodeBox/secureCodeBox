# Default values for dispatcher.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

webhookUrl: "http://example.com"

image:
  registry: docker.io
  repository: scbexperimental/teams-webhook
  tag: latest
  digest: null
