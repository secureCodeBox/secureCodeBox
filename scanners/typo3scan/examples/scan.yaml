
 apiVersion: "execution.securecodebox.io/v1"
 kind: Scan
 metadata:
   name: typo3scan-example
   spec:
   scanType: "typo3scan"
   parameters:
     - "-d"
     - "https://www.typo3example.com" # Change to the website you want to scan
     # Only show vulnuerable extensions
     - "--vuln"
     # Set the number of threads to use for enumerating extensions at 10
     - "--threads 10"