apiVersion: v1
kind: Service
metadata:
  name: {{ include "typo3scan.fullname" . }}
  labels:
    {{- include "typo3scan.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "typo3scan.selectorLabels" . | nindent 4 }}
