apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "typo3scan.fullname" . }}-test-connection"
  labels:
    {{- include "typo3scan.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "typo3scan.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
