version: "3"

includes:
  scanner:
    taskfile: ../Taskfile.yaml
    flatten: true
    excludes:
      - predeploy
    vars:
      scannerName: nikto

tasks:
  predeploy:
    deps:
      - demo-targets:deploy:bodgeit
    cmds: []
