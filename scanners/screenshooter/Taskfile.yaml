version: "3"

includes:
  scanner:
    taskfile: ../Taskfile.yaml
    flatten: true
    excludes:
      - predeploy
    vars:
      scannerName: screenshooter

tasks:
  predeploy:
    deps:
      - demo-targets:deploy:nginx
    cmds: []
