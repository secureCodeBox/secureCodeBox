apiVersion: "execution.securecodebox.io/v1"
kind: Scan
metadata:
  name: "zap-authenticated-full-scan-juiceshop"
  labels:
    organization: "OWASP"
spec:
  scanType: "zap-extended-full-scan"
  parameters:
    # target URL including the protocol
    - "-t"
    - "http://juiceshop.default.svc:3000/"
