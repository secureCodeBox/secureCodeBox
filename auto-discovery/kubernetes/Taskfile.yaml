# SPDX-FileCopyrightText: the secureCodeBox authors
#
# SPDX-License-Identifier: Apache-2.0

version: "3.48.0"

includes:
  base:
    taskfile: ../Taskfile.yaml
    vars:
      COMPONENT_NAME: kubernetes
      BINARY_NAME: manager
      BUILD_CMD: main.go

env:
  IMG_NS: securecodebox
  IMG_TAG:
    sh: 'echo "sha-$(git rev-parse --short HEAD)"'

vars:
  CONTROLLER_TOOLS_VERSION: v0.18.0
  ENVTEST_K8S_VERSION:
    sh: |
      cd {{ .TASKFILE_DIR }} && \
      v=$(go list -m -f '{{if .Replace}}{{.Replace.Version}}{{else}}{{.Version}}{{end}}' k8s.io/api 2>/dev/null); \
      printf '%s\n' "$v" | sed -E 's/^v?[0-9]+\.([0-9]+).*/1.\1/'
  ENVTEST_VERSION:
    sh: |
      cd {{ .TASKFILE_DIR }} && \
      v=$(go list -m -f '{{if .Replace}}{{.Replace.Version}}{{else}}{{.Version}}{{end}}' sigs.k8s.io/controller-runtime 2>/dev/null); \
      printf '%s\n' "$v" | sed -E 's/^v?([0-9]+)\.([0-9]+).*/release-\1.\2/'
  LOCALBIN: '{{ .TASKFILE_DIR }}/bin'

tasks:
  controller-gen:
    desc: "Download controller-gen locally if necessary"
    dir: '{{ .TASKFILE_DIR }}'
    cmds:
      - mkdir -p {{ .LOCALBIN }}
      - |
        CONTROLLER_GEN={{ .LOCALBIN }}/controller-gen
        CONTROLLER_TOOLS_VERSION={{ .CONTROLLER_TOOLS_VERSION }}
        echo "Downloading controller-gen@$CONTROLLER_TOOLS_VERSION"
        rm -f "$CONTROLLER_GEN" "$CONTROLLER_GEN-$CONTROLLER_TOOLS_VERSION"
        GOBIN={{ .LOCALBIN }} go install sigs.k8s.io/controller-tools/cmd/controller-gen@$CONTROLLER_TOOLS_VERSION
        mv "{{ .LOCALBIN }}/controller-gen" "$CONTROLLER_GEN-$CONTROLLER_TOOLS_VERSION"
        ln -sf "controller-gen-$CONTROLLER_TOOLS_VERSION" "$CONTROLLER_GEN"
    status:
      - test -f {{ .LOCALBIN }}/controller-gen-{{ .CONTROLLER_TOOLS_VERSION }}
      - test -L {{ .LOCALBIN }}/controller-gen
      - test "$(readlink {{ .LOCALBIN }}/controller-gen)" = "controller-gen-{{ .CONTROLLER_TOOLS_VERSION }}"

  manifests:
    desc: "Generate WebhookConfiguration, ClusterRole and CustomResourceDefinition objects"
    deps: [controller-gen]
    dir: '{{ .TASKFILE_DIR }}'
    cmds:
      - |
        {{ .LOCALBIN }}/controller-gen rbac:roleName=auto-discovery,headerFile="hack/boilerplate.yaml.txt" \
          crd paths="./..." \
          output:crd:artifacts:config=crds \
          output:rbac:artifacts:config=templates/rbac

  test:
    desc: "Run Go tests with manifests generation"
    deps: [manifests, base:fmt, base:vet, base:envtest]
    dir: '{{ .TASKFILE_DIR }}'
    cmds:
      - |
        KUBEBUILDER_ASSETS="$({{ .LOCALBIN }}/setup-envtest use {{ .ENVTEST_K8S_VERSION }} --bin-dir {{ .LOCALBIN }} -p path)" \
        go test ./... -coverprofile cover.out

  ginkgo:
    desc: "Run Ginkgo tests with verbose output and manifests generation"
    deps: [manifests, base:fmt, base:vet, base:envtest]
    dir: '{{ .TASKFILE_DIR }}'
    cmds:
      - |
        KUBEBUILDER_ASSETS="$({{ .LOCALBIN }}/setup-envtest use {{ .ENVTEST_K8S_VERSION }} --bin-dir {{ .LOCALBIN }} -p path)" \
        ginkgo test ./... -coverprofile cover.out -v --progress

  run:
    desc: "Run the kubernetes auto-discovery controller from your host"
    deps: [manifests, base:fmt, base:vet]
    dir: '{{ .TASKFILE_DIR }}'
    cmds:
      - go run ./main.go --config ./auto-discovery-config.yaml

  install:
    desc: "Install CRDs into the K8s cluster specified in ~/.kube/config"
    deps: [manifests]
    dir: '{{ .TASKFILE_DIR }}'
    cmds:
      - kubectl apply -f ./crds/

  uninstall:
    desc: "Uninstall CRDs from the K8s cluster specified in ~/.kube/config"
    deps: [manifests]
    dir: '{{ .TASKFILE_DIR }}'
    cmds:
      - kubectl delete -f ./crds/
