apiVersion: v2
name: engine
description: The engine is the central component of the secureCodeBox. It orchestrates the Security Test instances.

type: application

version: 0.1.0

appVersion: latest

dependencies:
  - name: redis
    version: 9.2.2
    repository: https://kubernetes-charts.storage.googleapis.com/
  - name: minio
    version: 2.5.16
    repository: https://kubernetes-charts.storage.googleapis.com/
