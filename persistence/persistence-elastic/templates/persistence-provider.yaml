apiVersion: 'experimental.securecodebox.io/v1'
kind: PersistenceProvider
metadata:
  name: 'persistence-elastic'
  labels:
    type: Structured
spec:
  address: "http://{{ include "persistence-elastic.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:3000"
